rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read/write globally for this passwordless friend league predictor, preventing any seeding or syncing permissions errors
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
